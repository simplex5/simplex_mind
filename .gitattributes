# Normalize line endings: LF in the repo, native never forced (Git Bash + Python
# both handle LF fine on Windows, and CRLF churn would pollute diffs across machines).
* text=auto eol=lf

# Never let autocrlf touch binaries (defensive; no .db is tracked today).
*.db binary
*.sqlite binary
*.png binary
*.jpg binary
